server:
  port: 8888
spring:
  cloud:
    config:
      server:
        svn:
          uri: https://subversion.assembla.com/svn/batusel.demo/
          passphrase: your-pass-phrase
          default-label: trunk
  profiles:
    active: subversion